On macOS, `app.paths.config` is now located in `~/Library/Application Support`, rather than `~/Library/Preferences`. Apple's guidelines reserve the Preferences folder for the system preferences API; apps should not create files there themselves.
